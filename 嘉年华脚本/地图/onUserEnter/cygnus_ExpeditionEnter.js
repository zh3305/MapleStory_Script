/*
 Made by Kent
 */

var status = -1;

function action(mode, type, selection) {
    var eim = ms.getEventInstance();
    if (eim != null) {
        //ms.getPlayer().sendDathCount();
    }
    ms.dispose();
}
